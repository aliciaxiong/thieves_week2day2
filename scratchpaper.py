
l_1 = [1,11,14,5,8,9]
def num_list(l_1):
      for num in l_1:
        if num < 10:
            return num_list(l_1)

l_1 = [1,2,3,4,5,6]
l_2 = [3,4,5,6,7,8,10]

def sortcode(main_list): 
    main_list = l_1 + l_2
    main_list.sort()
    return sortcode(main_list)

l_1 = [1,2,3,4,5,6]
l_2 = [3,4,5,6,7,8,10]

def sortcode(main_list): 
    if main_list in num == l_1 + l_2:
        num = main_list.sort()
        return main_list
    
    def sortcode(main_list): 
        main_list = (l_1 + l_2)
    return (main_list.sort)
    sortcode(main_list)

    def sortcode(main_list): 
        main_list = (l_1 + l_2)
        main_list.sort()
        return main_list